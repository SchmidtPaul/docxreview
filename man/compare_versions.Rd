% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_versions.R
\name{compare_versions}
\alias{compare_versions}
\title{Compare Two Versions of a Word Document}
\usage{
compare_versions(old_docx, new_docx, output_file = NULL)
}
\arguments{
\item{old_docx}{Path to the original (older) \code{.docx} file.}

\item{new_docx}{Path to the revised (newer) \code{.docx} file.}

\item{output_file}{Path for the comparison output \code{.docx}. If \code{NULL}
(default), a file named \verb{compared_<old>-vs-<new>.docx} is created in the
directory of \code{new_docx}.}
}
\value{
The path to the comparison document (invisibly).
}
\description{
Uses Microsoft Word's built-in document comparison to produce a new DOCX
with tracked changes showing all differences between an old and a new
version. This is useful after revising a Quarto/R Markdown source based on
reviewer feedback — the comparison document lets the reviewer see exactly
what changed without manually diffing.
}
\details{
\subsection{Prerequisites}{

This function requires \strong{all three} of the following:
\itemize{
\item \strong{Microsoft Word} installed (Windows only, uses COM automation)
\item \strong{Python} available on the system PATH
\item \strong{pywin32} Python package installed (\verb{pip install pywin32})
}

The function calls Python via \code{\link[=system2]{system2()}} to automate Word's
\code{CompareDocuments} feature. It is not available on macOS or Linux.

\strong{Note:} This function opens Microsoft Word in the background via COM
automation. If Word is already running with unsaved documents, those
documents are not affected. Word is only closed if no other documents
remain open after the comparison.
}
}
\examples{
\dontrun{
# Auto-generated output path
compare_versions("report_v1.docx", "report_v2.docx")

# Explicit output path
compare_versions("report_v1.docx", "report_v2.docx", "comparison.docx")
}
}
